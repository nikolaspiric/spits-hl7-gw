server:
  port: 8080
  error:
    whitelabel:
      enabled: false

spring:
  jpa:
    open-in-view: false
  liquibase:
    change-log: classpath:/db/db.changelog-master.xml

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spits_db?useSSL=false&serverTimezone=UTC&maxReconnects=10&autoReconnect=true&nullNamePatternMatchesAll=true
    username: root
    password: nikola
